<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="finalproject.cpsc471_dbms">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_book_2"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.activities.LoginActivity"
            android:windowSoftInputMode="stateHidden" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".UI.activities.MainActivity"
            android:windowSoftInputMode="stateHidden" />

        <activity android:name=".UI.activities.AddItemActivity"
            android:parentActivityName=".UI.activities.MainActivity"
            android:windowSoftInputMode="stateHidden"/>

        <activity android:name=".UI.activities.CreateEventActivity"
            android:parentActivityName=".UI.activities.MainActivity"
            android:windowSoftInputMode="stateHidden"/>

        <activity android:name=".UI.activities.CreateSponsorActivity"
            android:parentActivityName=".UI.activities.CreateUserActivity"/>

        <activity android:name=".UI.activities.CreateUserActivity"
            android:parentActivityName=".UI.activities.LoginActivity"
            android:windowSoftInputMode="stateHidden"/>

        <activity android:name=".UI.activities.DonateBooksActivity"
            android:parentActivityName=".UI.activities.MainActivity"/>

        <activity android:name=".UI.activities.EditUserActivity"
            android:parentActivityName=".UI.activities.MainActivity"
            android:windowSoftInputMode="stateHidden"/>

        <activity android:name=".UI.activities.EventViewActivity"
            android:parentActivityName=".UI.activities.MainActivity"/>

        <activity android:name=".UI.activities.MaterialViewActivity"
            android:parentActivityName=".UI.activities.MainActivity"/>

        <activity android:name=".UI.activities.RateActivity"
            android:parentActivityName=".UI.activities.MainActivity"/>

    </application>

</manifest>